apiVersion: acme.com/v1
kind: Workload
metadata:
name: workload
image: wingcloudbot/kblocks-ui-app:sha-{{ .Values.revision }}
port: 5678
route: /kblocks-ui-app(/|$)(.*)
rewrite: /$2
replicas: 2
env:
ECHO_TEXT: "hello from kblocks-ui-app"
