apiVersion: acme.com/v1
kind: Workload
metadata:
name: workload
image: wingcloudbot/kblocks-test-app:sha-{{ .Values.revision }}
port: 5678
route: /kblocks-test-app(/|$)(.*)
rewrite: /$2
replicas: 2
env:
ECHO_TEXT: "hello from kblocks-test-app"
